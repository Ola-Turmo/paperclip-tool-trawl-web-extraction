{
  "name": "uos-tool-trawl-web-extraction",
  "version": "0.1.0",
  "description": "Intelligent Web Intelligence Platform — extract structured knowledge from any website, detect changes, turn the web into a queryable knowledge base",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "vitest",
    "lint": "eslint src --ext .ts"
  },
  "dependencies": {
    "zod": "^3.22.0",
    "playwright": "^1.40.0",
    "@paperclipai/plugin-sdk": "latest"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "@types/node": "^20.0.0",
    "vitest": "^1.0.0",
    "eslint": "^8.0.0"
  }
}
